<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="ThemeBOA" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBOA</item>
        <item name="colorPrimaryDark">@color/colorBOADark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeTechnical" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorTechnical</item>
        <item name="colorPrimaryDark">@color/colorTechnicalDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeFun" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorFun</item>
        <item name="colorPrimaryDark">@color/colorFunDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeCultural" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorCultural</item>
        <item name="colorPrimaryDark">@color/colorCulturalDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeSponsored" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorSponsored</item>
        <item name="colorPrimaryDark">@color/colorSponsoredDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThemeLiterary" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorLiterary</item>
        <item name="colorPrimaryDark">@color/colorLiteraryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="CustomText" parent="TextAppearance.AppCompat">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000</item>
        <item name="android:fontFamily">@font/oxygen_light</item>
        <item name="android:includeFontPadding">false</item>

    </style>
</resources>
